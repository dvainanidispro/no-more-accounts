# No more accounts — Τεχνική τεκμηρίωση

Τεκμηρίωση για προγραμματιστές. Για τη γενική περιγραφή δείτε το [README](README.md).

## Αρχιτεκτονική

Το plugin εφαρμόζει προστασία σε **δύο επίπεδα**:

| Επίπεδο | Μηχανισμός | Τι καλύπτει |
|---|---|---|
| WordPress | Filters/hooks (`includes/prevent.php`) | Όλες τις ροές που περνούν από το WordPress (wp-admin, REST, φόρμες εγγραφής, plugins) |
| Βάση δεδομένων | SQL triggers (`includes/triggers.php`) | Ό,τι παρακάμπτει το WordPress (απευθείας SQL, SQL injection, άλλα scripts στην ίδια βάση) |

Βασική φιλοσοφία: τα μπλοκαρίσματα είναι **διακριτικά**. Όλα τα μηνύματα σφάλματος είναι γενικά/αόριστα και δεν αποκαλύπτουν την ύπαρξη του plugin, ώστε ένας επιτιθέμενος να μην καταλαβαίνει τι τον σταμάτησε (ή, σε κάποιες περιπτώσεις, ούτε καν ότι σταματήθηκε).

## Options

Όλα αποθηκεύονται ως string `'1'` / `'0'` (ώστε να διαβάζονται εύκολα και από τα SQL triggers) και δημιουργούνται με προεπιλογή `'0'` στην ενεργοποίηση του plugin (και συμπληρωματικά σε κάθε εμφάνιση της σελίδας ρυθμίσεων). Διαγράφονται στην απεγκατάσταση.

| Option | Λειτουργία |
|---|---|
| `nma_prevent_api_users` | Αποτροπή δημιουργίας χρηστών μέσω REST API + απόκρυψη endpoint χρηστών από μη εξουσιοδοτημένα αιτήματα |
| `nma_prevent_users` | Αποτροπή δημιουργίας νέων χρηστών (χρησιμοποιείται από το WP filter **και** από το trigger του πίνακα χρηστών) |
| `nma_prevent_admins` | Αποτροπή απόδοσης του ρόλου administrator (χρησιμοποιείται από τα triggers του usermeta) |

Στη φόρμα ρυθμίσεων τα `nma_prevent_users`/`nma_prevent_admins` εμφανίζονται ως **ένα radio** τριών τιμών (none/users/admins), επειδή δεν έχει νόημα να είναι ενεργά ταυτόχρονα — το radio είναι καθαρά θέμα UI, τα options στη βάση παραμένουν δύο ανεξάρτητες εγγραφές. Αν βρεθούν και τα δύο `'1'` (π.χ. από απευθείας επέμβαση στη βάση), στην εμφάνιση προηγείται το «users» και το πρώτο σώσιμο τα κανονικοποιεί.

Helpers: `nma_option_enabled( $option )`, `nma_user_protection_active()` (option + trigger χρηστών OK), `nma_admin_protection_active()` (option + τα δύο usermeta triggers OK).

## SQL Triggers

Τρία **στατικά** triggers — δεν αλλάζουν ποτέ· η συμπεριφορά ελέγχεται μόνο από τα options που διαβάζουν σε χρόνο εκτέλεσης. Τα ονόματα περιέχουν το table prefix για να μη συγκρούονται με δεύτερη εγκατάσταση WordPress στην ίδια βάση:

1. **`{prefix}nma_before_insert_user`** — `BEFORE INSERT` στον `{prefix}users`. Αν `nma_prevent_users = '1'`, ακυρώνει το INSERT με `SIGNAL SQLSTATE '45000'`.
2. **`{prefix}nma_before_insert_usermeta`** — `BEFORE INSERT` στον `{prefix}usermeta`. Αν η νέα γραμμή είναι `meta_key = '{prefix}capabilities'` με `meta_value LIKE '%"administrator";b:1%'` και `nma_prevent_admins = '1'`, ακυρώνει το INSERT.
3. **`{prefix}nma_before_update_usermeta`** — `BEFORE UPDATE` στον `{prefix}usermeta`. Μπλοκάρει μόνο τη **μετάβαση** σε administrator (το `OLD.meta_value` δεν περιείχε τον ρόλο, το `NEW` τον περιέχει) — έτσι οι ενημερώσεις capabilities υπαρχόντων administrators δεν επηρεάζονται.

Σχεδιαστικές λεπτομέρειες:

- **Μήνυμα σφάλματος:** `MESSAGE_TEXT = 'Unknown error'` — σκόπιμα αόριστο. Όποιος μιλά απευθείας στη MySQL βλέπει `ERROR 1644 (45000): Unknown error`. Το 1644 προδίδει ότι κάποιο trigger/procedure πέταξε custom σφάλμα, αλλά όχι ποιο ή γιατί.
- **Fail-open:** αν η εγγραφή option λείπει από τον πίνακα options, η σύγκριση με `NULL` αποτυγχάνει και η εγγραφή **επιτρέπεται**. Δεν κλειδώνεσαι έξω από λάθος.
- **Pattern ρόλου:** το `'%"administrator";b:1%'` ταιριάζει το serialized κλειδί του ρόλου με τα εισαγωγικά του (δεν πιάνει π.χ. custom ρόλο `administrator_assistant`) και μόνο όταν είναι ενεργός (`b:1`). Καλύπτει και arrays πολλών ρόλων, με οποιαδήποτε σειρά.
- **Απόδοση:** ο `usermeta` δέχεται συχνές εγγραφές (session tokens κ.λπ.), γι' αυτό τα usermeta triggers ελέγχουν πρώτα `meta_key`/`meta_value` με φωλιασμένα IF και διαβάζουν το option **μόνο** για γραμμές capabilities με administrator. Το trigger του `users` κάνει ένα indexed lookup στο `option_name` ανά (σπάνιο) INSERT.
- **Κύκλος ζωής:** τα triggers εγκαθίστανται **μόνο** από τη σελίδα ρυθμίσεων (κουμπί «Εγκατάσταση / επιδιόρθωση», idempotent — κάνει πρώτα `DROP TRIGGER IF EXISTS`). Αφαιρούνται στην **απενεργοποίηση** (απενεργοποιημένο plugin = απενεργοποιημένη προστασία, αλλιώς θα μπλόκαρε χωρίς διαθέσιμο UI) και στην **απεγκατάσταση**. Η ενεργοποίηση δημιουργεί μόνο τα options.
- **Έλεγχος κατάστασης:** μέσω `information_schema.TRIGGERS` (σωστός πίνακας, timing, event) και ελέγχου ότι το σώμα περιέχει τα αναμενόμενα τμήματα (option, pattern, SIGNAL, μήνυμα). Επιστρέφει `ok` / `missing` / `different` — το `different` εμφανίζεται στη σελίδα ρυθμίσεων ως «προτείνεται επιδιόρθωση» (π.χ. μετά από αλλαγή του SQL σε νέα έκδοση του plugin).
- Η σελίδα ρυθμίσεων ελέγχει το δικαίωμα `TRIGGER` του χρήστη της βάσης με `SHOW GRANTS FOR CURRENT_USER()` (αναγνωρίζει `TRIGGER` ή `ALL PRIVILEGES`, καθολικά ή στην τρέχουσα βάση, με υποστήριξη wildcards στο όνομα βάσης).

## Προστασία σε επίπεδο WordPress (`includes/prevent.php`)

### Καθολικό μπλόκο δημιουργίας χρηστών

Filter στο `wp_pre_insert_user_data` (priority 999, ώστε να τρέχει τελευταίο): όταν `nma_user_protection_active()` και πρόκειται για **δημιουργία** (όχι ενημέρωση), επιστρέφει κενό array. Ο ίδιος ο core τότε σταματά **πριν** το INSERT και επιστρέφει το δικό του γενικό `WP_Error( 'empty_data', 'Not enough data to create this user.' )`. Επειδή όλες οι ροές δημιουργίας χρήστη (wp-admin, REST, `wp_create_user()`, WooCommerce, φόρμες εγγραφής) περνούν από την `wp_insert_user()`, ένα σημείο καλύπτει τα πάντα, με αυθεντικό μήνυμα του core.

### Καταστολή ειδοποιήσεων για ανύπαρκτο χρήστη

Ο core (στο `edit_user()`) πυροδοτεί το action αποστολής ειδοποιήσεων νέου χρήστη **ακόμα και όταν** η `wp_insert_user()` επιστρέψει `WP_Error`, με αποτέλεσμα η `wp_new_user_notification()` να τρέχει με `$user = false` (PHP warnings + άσκοπο email «New User Registration»). Δύο filters (`wp_send_new_user_notification_to_admin` / `_to_user`) ακυρώνουν την αποστολή όταν ο χρήστης δεν είναι έγκυρος `WP_User`. Ο έλεγχος είναι γενικός (δεν κοιτά αν έφταιξε το plugin) — email για ανύπαρκτο χρήστη δεν πρέπει να φεύγει ποτέ.

### REST API

Filter στο `rest_endpoints` όταν `nma_prevent_api_users = '1'`:

- **Μη εξουσιοδοτημένα αιτήματα:** αφαιρούνται όλες οι διαδρομές `/wp/v2/users*`. Απάντηση: το αυθεντικό `rest_no_route` (404) του core· ούτε το discovery (`/wp-json/wp/v2`) τις αναφέρει. Παράπλευρο όφελος: κόβεται το user enumeration μέσω του ανώνυμου `GET /wp/v2/users` (λίστα συντακτών δημοσιευμένων άρθρων).
- **Εξουσιοδοτημένα αιτήματα:** αφαιρείται μόνο ο POST handler (δημιουργία), ώστε να λειτουργούν όσα admin features διαβάζουν χρήστες μέσω REST (π.χ. dropdown συντάκτη του Gutenberg).

**Προσοχή στη σημασιολογία του «συνδεδεμένος»:** στο REST, cookie-authenticated αίτημα **χωρίς nonce** (`X-WP-Nonce`) αντιμετωπίζεται από τον core ως ανώνυμο (`rest_cookie_check_errors()` → `wp_set_current_user(0)`). Άρα ακόμα και συνδεδεμένος admin που ανοίγει το `/wp-json/wp/v2/users` σκέτο στον browser βλέπει 404. Ορατό παραμένει μόνο για αιτήματα με nonce (wp-admin) ή Application Password.

Σημείωση: αρχικά η δημιουργία μέσω REST μπλοκαριζόταν με ξεχωριστό handler που επέστρεφε το γενικό σφάλμα του core· αντικαταστάθηκε από τον συνδυασμό «καθολικό `wp_pre_insert_user_data` + αφαίρεση endpoint». Το `rest_pre_insert_user` **δεν** χρησιμοποιήθηκε σκόπιμα: ο core δεν ελέγχει για `WP_Error` στην επιστροφή του (σε αντίθεση με το αντίστοιχο των posts).

### Admin notices (`admin/notices.php`)

Στη σελίδα «Προσθήκη χρήστη» (screen id `user`) εμφανίζεται διακριτικό warning «Η δημιουργία χρηστών είναι μπλοκαρισμένη.» ή «Η δημιουργία administrators είναι μπλοκαρισμένη.» — μόνο όταν η αντίστοιχη προστασία είναι **πραγματικά** ενεργή (option + triggers OK) και χωρίς αναφορά στο plugin.

## Ανεπιθύμητες ενέργειες & edge cases

- **«Κέλυφος» χρήστη χωρίς ρόλο:** όταν είναι ενεργή μόνο η αποτροπή administrators και επιχειρηθεί δημιουργία νέου χρήστη με ρόλο administrator, η εγγραφή στον `users` προηγείται της εγγραφής capabilities στον `usermeta` και **δεν υπάρχει κοινό transaction** για να ακυρωθεί συνολικά. Αποτέλεσμα: ο χρήστης δημιουργείται αλλά μένει **χωρίς κανέναν ρόλο** (μπορεί να συνδεθεί, δεν μπορεί να κάνει τίποτα). Πρέπει να διαγραφεί χειροκίνητα. Επίσης το (deprecated) meta `wp_user_level` μπορεί να γραφτεί κανονικά — ακίνδυνο χωρίς capabilities.
- **Σιωπηλές αποτυχίες:** με ενεργή την αποτροπή administrators, η αλλαγή ρόλου υπάρχοντος χρήστη σε administrator από το wp-admin δείχνει «επιτυχία» αλλά ο ρόλος δεν αλλάζει (η αποτυχία του UPDATE στο usermeta δεν ελέγχεται από τον core). Αυτό είναι συνειδητή επιλογή (σύγχυση επιτιθέμενων), αλλά μπορεί να μπερδέψει και νόμιμο διαχειριστή που δεν θυμάται τη ρύθμιση — εξ ου και τα admin notices.
- **Νόμιμα εργαλεία επηρεάζονται εξίσου:** setup wizards, migration/import plugins, WP-CLI (`wp user create`) — οτιδήποτε δημιουργεί χρήστες ή αποδίδει ρόλο administrator θα αποτυγχάνει (συχνά σιωπηλά) όσο οι προστασίες είναι ενεργές. Απενεργοποιήστε προσωρινά την αντίστοιχη ρύθμιση πριν από migrations/imports.
- **Δυσκολία στο debugging:** το «Unknown error» είναι σκόπιμα μη αναγνωρίσιμο — αν το ξεχάσετε, μια αποτυχία INSERT/UPDATE από άλλο εργαλείο μπορεί να σας παιδέψει. Πρώτος ύποπτος σε τέτοια σφάλματα είναι αυτό το plugin.
- **Binary logging:** σε MySQL με ενεργό binlog χωρίς `SUPER`, η δημιουργία triggers μπορεί να απαιτεί `log_bin_trust_function_creators = 1` (error 1419). Το σφάλμα εμφανίζεται αυτούσιο στη σελίδα ρυθμίσεων.
- **Ορατότητα των triggers:** όποιος έχει πρόσβαση στο `information_schema`/`SHOW TRIGGERS` της βάσης βλέπει τα ονόματα και το σώμα τους (περιέχουν το πρόθεμα `nma_`). Η «αορατότητα» αφορά τα κανάλια του WordPress/HTTP, όχι τη βάση καθαυτή.
- **Logging από τα triggers δεν είναι εφικτό:** το `SIGNAL` προκαλεί rollback όλων των ενεργειών του statement, συμπεριλαμβανομένων εγγραφών log που θα έκανε το ίδιο το trigger.

## Συνειδητές απλοποιήσεις / εκτός εμβέλειας

- **Multisite:** δεν υποστηρίζεται/δοκιμάστηκε. Χρησιμοποιείται παντού το `base_prefix` και δεν καλύπτονται οι ροές `wpmu_create_user()` / network admin.
- **Logging:** αποφασίστηκε συνειδητά να μην υπάρχει καμία καταγραφή μπλοκαρισμένων αποπειρών (μηδενικό ίχνος και για εμάς).
- **REST endpoints τρίτων:** το `nma_prevent_api_users` αφορά μόνο το core `/wp/v2/users`. Endpoints όπως το WooCommerce `/wc/v3/customers` δεν καλύπτονται από αυτό — καλύπτονται όμως από το καθολικό μπλόκο του `nma_prevent_users` όταν εκείνο είναι ενεργό.
- **Το dropdown ρόλων δεν φιλτράρεται:** ο ρόλος «Διαχειριστής» παραμένει επιλέξιμος στα Δημιουργία/Επεξεργασία χρήστη ακόμα κι όταν η απόδοσή του μπλοκάρεται (συνειδητή επιλογή — προτιμήθηκε το σιωπηλό fail από την τροποποίηση του UI).
- **Μόνο ο ρόλος `administrator`:** η αποτροπή αφορά αποκλειστικά αυτόν· άλλοι «ευαίσθητοι» ρόλοι (π.χ. `shop_manager`, custom ρόλοι με `manage_options`) δεν ελέγχονται.
- **User enumeration από άλλα κανάλια:** καλύπτεται μόνο το REST. Author archives (`?author=N`), sitemaps κ.λπ. είναι εκτός εμβέλειας.
- **UI μόνο στα Ελληνικά:** τα strings είναι hardcoded, χωρίς i18n/text domain loading.

## Πιθανές μελλοντικές βελτιώσεις

- Προαιρετικό logging μπλοκαρισμένων αποπειρών (σε option στη βάση, με πίνακα «πρόσφατες απόπειρες» στη σελίδα ρυθμίσεων, σε αρχείο που να φαίνεται και το "ύποπτο" email).
- Επέκταση της αποτροπής administrators σε παραμετροποιήσιμη λίστα ρόλων ή/και σε capabilities (π.χ. `manage_options`).
- Προαιρετικό φιλτράρισμα του ρόλου «Διαχειριστής» από τα dropdowns (`editable_roles`) για καθαρότερη εμπειρία νόμιμων διαχειριστών.
- Κάλυψη REST endpoints τρίτων (π.χ. WooCommerce customers) από το option του API.
- Προβολή του **αποθηκευμένου** σώματος των triggers δίπλα στο αναμενόμενο SQL, όταν η κατάσταση είναι «διαφέρει», για εύκολη σύγκριση.
- Δίγλωσσο UI (Ελληνικά/Αγγλικά) μέσω i18n.
- Υποστήριξη multisite.
- Αυτόματος καθαρισμός «κελυφών» (χρηστών χωρίς ρόλο) που δημιουργούνται από μπλοκαρισμένες απόπειρες δημιουργίας administrator.
- Αυτόματη διαγραφή χρηστών οι οποίοι είναι εκτός των επιτρεπόμενων οι οποίοι δηλώθηκαν στο plugin (πχ με cron job). 
